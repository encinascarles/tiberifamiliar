export const publicRoutes = ["/", "/perfil", "/verificacio"];
export const authRoutes = ["/login", "/register", "/autherror"];
export const apiAuthPrefix = "/api/auth";
export const DEFAULT_LOGIN_REDIRECT = "/";
